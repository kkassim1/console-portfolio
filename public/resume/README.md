# Résumé drop location

Add your finished résumé PDF here using this exact filename:

```text
kwam-kassim-resume.pdf
```

When the file is present, update the `resume` entry in `src/projects.js` with:

```js
links: [
  { label: 'View Résumé', url: '/resume/kwam-kassim-resume.pdf' },
],
```

Keep the PDF free of private contact information that you do not want publicly available.
